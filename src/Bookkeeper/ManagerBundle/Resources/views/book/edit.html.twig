{% extends '::base.html.twig' %}

{% block body -%}

    <h1>Edit</h1>

    <div class="col-xs-4">
    {{ form(form) }}
    </div>

    <div class="col-xs-12">
    <ul>
        <li><a href="{{ path('book') }}">Book manager</a></li>
    </ul>
    </div>

{% endblock -%}